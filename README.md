# 刷题记录

## day 1

- 160.相交链表：会做但不是最优解，比较优的解法是双指针。
- 236.二叉树的最近公共祖先：这个自己的思路是对的，但是可以改造原本的函数
- 234.回文链表：这个有两种思路，各有优劣。自己的思路是拷贝一份到std::vector，然后判断是否为回文链表。另一种思路是直接原地反转前一半的链表，这样的话就不需要额外的空间了，但是会改变原本链表的结构，需要再修改回来。
- 739.每日温度：主要是考察单调栈的使用，这个一开始是没有思路的，需要记住

## day 2

- 226.翻转二叉树：这个很简单，基本的递归就可以了。
- 221.最大正方形：经典的动态规划题目，可以用来理解dp的原理，一定要写熟练。
- 215.数组中的第K个最大元素：这道题应该会用快排的思想，然后简单一点就使用堆来做
- 208.实现Trie（前缀树）：这题目很简单，但是就是容易写小bug，主要是需要熟练度

## day 3

- 207.课程表：这道题主要是用到了拓扑排序的思路，记住即可。另外自己的解法里面一开始没有好好读题目，因此用到了集合，这里碰巧了解了一下集合的交集以及并集的使用方法。
- 206.反转链表：这道题很简单，但是一定要写熟练了，主要是有点点绕。这里深刻体会到了头节点的方便性，一定要把没有头节点的链表全都转换成为有头节点的链表。
- 200.岛屿数量：这里自己写的解法栈溢出了，再想想。。。。。   好了，做出来了，这里只要最简单的dfs就好了，不需要太复杂。

## day 4

- 198.打家劫舍：这道题就是一个简单的动态规划，只需要记住一个点，找出当前范围内的最大可抢劫数，然后逐渐往后面累积即可。
- 169.多数元素：这道题似乎有一个众数的获取算法，我这里使用的是第k大元素这个思路，最优的情况下，时间复杂度是O(N)，似乎也可以，但是最坏的情况下需要O(N^2)的复杂度吧？似乎有点不太对，这里应该学习一下众数选举的算法。

## day 5

- 139.单词拆分：这个题目主要是需要熟练手写前缀树，然后要用一点动态规划的思路来做这道题。
- 141.环形链表：这个题目主要是需要记住数学推导，双指针很简单。
- 142.环形链表II：这个题目也是需要记住数学推导。
- 146.LRU缓存：这个题目主要是要熟练的写双链表，单链表为了方便操作第一个节点，往往会加上一个冗余的头节点，而双链表则可以再加上一个冗余的尾节点用来方便操作最后一个节点。然后就是要熟练封装，这样会比较容易来实现双链表的各项功能。

## day 6

- 136.只出现过一次的数字：这道题目记住就可以了，直接使用异或来做就可以。
- 238.除了自身以外数组的乘积：这道题目的关键在于需要将数组划分成左右两半，这样的情况下左边以及右边就都是连续变长的，这样就方便使用dp来做了。
- 155.最小栈：这道题目很简单，就是用一个额外的栈就可以做到了。
- 152.乘积最大的子数组：这道题目比较烧脑，因为可能里面会有负数和0,因此需要同时维护一个最大连续乘积以及最小连续乘积。
- 148.排序链表：链表排序非常适合使用归并排序来做，而且关于没有头节点的链表，一定要给它分配一个冗余头节点，这样会非常方便操作，最后可以把冗余的节点再释放掉就是了。不过这道题目没有做到最优，因为使用的是自顶向下的归并，依赖了递归，空间复杂度不是最优，后面应该尝试一下使用自底向上的方法。

## day 7

- 647.回文子串：这道题目直接使用中心扩展法，思路还是很简单的，就是要注意一个点：不要轻易使用size_t，因为有可能会需要逆序遍历，这个时候>=0这个条件往往会成为隐患，因为无符号整型永远大于等于0，正向遍历的时候可以使用。
- 128.最长连续序列：这道题应该学会巧用hashset。
- 461.汉明距离：这道题目直接使用异或来做就可以了，最后统计一下1的个数即可。

 ## day 8

- 494.目标和：这道题目实际上可以转换成这样的题目：给定一个数组，任意选其中的n个元素，结果是目标和，一共有几种方案？典型的动态规划题目。

 ## day 9

- 124.二叉树中的最大路径和：这道题目一开始的思路是对的，就是递归先去求取一个节点单边的最大值，加上该节点的值，这样逐渐向上传递，中间用一个ans来持续更新遇到的最大值即可。
- 438.找到字符串中所有字母异位词：思想是滑动窗口，但是自己写的太烂了，需要学习一下官方题解。
- 437.路径总和III：这道题目暂时用的是O(N^2)的复杂度，后面应该要尝试一下用前缀和来解决。

 ## day 10

- 416.分割等和子集：又是一种0-1背包问题。对于二维数组的写法需要理解，对于一维数组的写法尽量背过。

## day 11

- 406.根据身高重建队列：这道题目也是思路题，记住思路即可。
- 704.二分查找：无他，唯手熟耳。
- 27.移除元素：双指针法的一个典型案例。
- 977.有序数组的平方：也是双指针法比较好做，一开始自己也是这个思路，但是一开始自己想的是正序插入数据，后面发现其实可以倒序计算结果。
- lcr008.长度最小的子数组：这道题目是一个滑动窗口的经典题目，滑动窗口可以取代两层for循环，从而达到O(N)的时间复杂度，一定要理解在什么时候需要滑动左边，什么时候需要滑动右边。
- 59.螺旋矩阵II：这道题目是一道典型的模拟题，需要思路清晰，逻辑以及条理一定要好。
- 203.移除链表元素：比较简单的题目，遇到不带头节点的链表，我们可以手动添加一个头节点，这样可以统一所有的节点的处理逻辑，最后再把头节点给删除掉即可。
- 24.两两交换链表中的节点：还是加上头节点，然后思路很简单，最后把头节点删除掉即可。
- 19.删除链表中的倒数第N个结点：还是加上头节点，然后用快慢指针法就可以做了。
- 242.有效的字母异位词：这道题主要就是考察使用哈希表，思路还是比较简单的。
- 349.两个数组的交集：这道题可以使用set来做，其实本质也是使用的哈希表。
- 202.快乐数：这道题也是一个使用set的典型例子。

## day 12

- 1.两数之和：思路很简单，只需要一个哈希表就可以了。
- 454.四数相加II：每两个数组相结合组成一个哈希表，然后时间复杂度就只有O(N^2)
- 383.赎金信：这道题和异构词那道题很像，基本上就是一个思路，但是这道题两个单词的长度可以不相同。
- 15.三数之和：找对了思路之后，一遍过！爽！无他，唯手熟耳！
- 18.四数之和：思路正确依旧是一遍过，就是int类型溢出了，这里问题不大。
- 344.反转字符串：这道题目依旧是很简单的思路，不过多赘述了。
- 541.反转字符串II：依然是很简单的思路，控制好边界条件即可。
- 151.反转字符串中的单词：记住std::reverse()的使用方式即可，另外就是注意右侧区间为开区间。
- 28.找出字符串中第一个匹配项的下标：KMP算法，没有什么好说的，记住算法且理解算法。这道题还没有过，要记住，一定要再写一遍。

## day 13

- 459.重复的子字符串：没什么技巧，还以为有什么比较好的方法来做，实际上就是暴力就可以了。
- 232.用栈实现队列：用两个栈来实现，思路很简单，就是利用栈的倒序性。
- 225.用队列实现栈：用两个队列来实现，需要注意栈pop出来的结果应该是队列的最后一个元素。
- 20.有效的括号：经典题目，用栈来做即可。
- 1047.删除字符串中的所有相邻重复项：记住这个思路，依然是使用栈来做。其实和括号匹配是差不多的一类题目。
- lcr036.逆波兰表达式求值：没什么好说的，一就是经典题目经典套路。
- 239.滑动窗口最大值：这里就是用一个优先队列，多存储一个index即可解决。
- 347.前K个高频元素：思路很简单，就是用堆来做即可。
- 144.二叉树的前序遍历：力扣的返回值有点别扭，别的还好。另外，还学习了使用迭代遍历前序。
- 94.二叉树的中序遍历：这道题使用迭代的思路自己居然一下子想出来了。

## day 14
- 145.二叉树的后序遍历：迭代的思路不太好想，但是实际上很简单，只需要把前序遍历的左右顺序颠倒，然后最后把ans数组reverse一下即可。
- 102.二叉树的层序遍历：思路很简单，就是使用queue来辅助即可。
- 107.二叉树的层序遍历II：调转入队列的左右顺序，然后最后用一个栈来辅助存储，最后按层次输出栈里面的数据即可。
- 199.二叉树的右视图：这道题目依旧很简单，就是找出每一层的最右边一个元素即可，因此入队列的时候还要保存level信息。
- 101.对称二叉树：一开始想着用层次遍历来做，但是实际上不对，这里应该用递归来做，也可以用一个队列来迭代做，其实本质是一样的，那就是，对于两棵二叉树，左边的左是右边的右，左边的右是右边的左，按照这样的思路很容易遍历确定是否是对称的。
- 104.二叉树的最大深度：使用层次遍历来做，就是广度优先，最后遍历一遍就可以自然得到最大深度，好像也可以用深度优先遍历，但是也是要遍历一轮。
- 111.二叉树的最小深度：使用层次遍历来做，这里只要使用层次遍历找到了第一个叶子节点，那这个叶子节点就是就是最小的深度了，可以直接结束。
- 222.完全二叉树的节点个数：可以使用遍历，但是更快的方式是使用完全二叉树的性质来做。
- 110.平衡二叉树：还以为自己的做法不是最优呢，但是似乎已经是最优了。就这样吧。
- 257.二叉树的所有路径：这里使用迭代来做的，用的方法是层次遍历。好像也是可以使用递归来做的，但是没有去考察了，先不管了，自己的思路也是最优的。
- 404.左叶子之和：还是递归来做即可，思路还是比较简单的。

## day 15

- 112.路径总和：这道题就用递归就可以，当然用层次遍历也没毛病。
- 513.找树左下角的值：层次遍历很好做，一定要能够熟练的写层次遍历。
- 98.验证二叉搜索树：这道题目一开始想简单了，上界和下界不管是左孩子还是右孩子都应该在这个范围里边。
- 530.二叉搜索树的最小绝对差：主要是需要知道如何在二叉搜索树中找到一个节点的前驱以及后继节点。
- 501.二叉搜索树中的众数：用前驱节点记录，然后需要记住如何一次遍历找一个有序数组的众数。
- 235.二叉搜索树的公共祖先：和236的思路一致，只不过left以及right可以通过二叉搜索树的判断免去一次递归。
- 450.删除二叉搜索树中的节点：应该要注意左右的顺序一定要正确，不然出了bug很难排查。
- 669.修剪二叉搜索树：思路理顺了其实就是一个简单的递归。
- 108.将有序数组转换为二叉搜索树：思路一下就想到了，最快的方式就是直接利用有序数组的中间元素作为根节点，然后递归构造即可。
- 538.把二叉搜索树转换为累加树：代码非常简洁，就是利用一个sum来累加，然后，使用右中左的中序遍历可以逆序来遍历一遍二叉树，这样每个节点都累加起来就可以了。

## day 16

- 77.组合：经典的回溯题目，回溯的关键就是push_back与pop_back两者夹住递归子函数。
- 216.组合总和III：和上一个题目是一样的思路，剪枝的条件可能稍微不一样罢了。
- 17.电话号码的字母组合：也是一道经典的回溯题目，自己对回溯的套路好像已经似懂非懂了。
- 39.组合总和：这里和前面不一样的是，可以使用重复的元素，因此下一层递归的start_index的值可以从当前开始，而不是从下一个开始。
- 40.组合总和II：这道题有点致敬三数之和以及四数之和的感觉了，先排序后跳过重复的，然后再来处理。
- 131.分割回文串：这道题依旧是回溯法，这里回溯的内容很好想，不过自己的做法还不是最优，最优的话应该还要用动态规划来判断是否为回文串，这里因为还没有做到dp的题目，暂且不去考虑。
- 93.复原IP地址：这道题一开始没有注意到1.1.1.02这种是不合法的，要注意一下。
- 78.子集：这道题也是常规的回溯，现在已经对回溯有了基本的掌握了，框架已经比较熟悉了。
- 90.子集II：和三数之和以及四数之和一样的处理方式，主要是先排序，然后跳过重复的。
- 491.非递减子序列：这里因为数组不能排序，因此去重必须要使用一个set来做了，不能直接使用原来的方法了。
- 46.全排列：也是一个经典的回溯题目，只不过不需要每一次都start_index前进了，需要标记使用过的元素，使用一个used数组来做到这一点。
- 47.全排列II：这里自己写出了死循环，原因在于，去重的逻辑是自己手动控制i++，但是一开始used[i]处是一个分支，这里也需要手动添加i++才对，总之一旦要是自己接管了for循环里的自增逻辑，那就要对所有的分支都要应用，否则就会出错。
- 106.从中序与后序遍历序列构造二叉树：这道题目用索引来做可以省去很多创建数组的开销，这道题目主要是要找好了索引。
- 654.最大二叉树：自己首先是用递归做的，但是不是最优解，这里学习了一下官方解法，很快看懂了，用单调栈是最优的，但是为什么要这么用还是很难说明白，这里只能说是记住这个题目，如果有类似的题目可以去套用。
- 617.合并二叉树：很简单的递归题目，没有什么好说的。
- 700.二叉搜索树中的搜索：二叉搜索树的题目，仍然是常规的递归。

## day 17

- 51.N皇后：这道题很经典，思路很清晰，就是写起来容易有小bug。学习了官方解法的做法，这里写起来更加简练了，而且剪枝之后效率更高。
- 37.解数独：这道题并不是传统的ans+path的题目，最后的结果直接在数组上面修改，因此helper要有一个返回值，如果返回值为true那么就不要再回溯了，这时候就已经找到了结果了。至此回溯题目完全结束，感觉回溯真的就是常规的模板题目，考虑好了边界之后还是不难的。
- 455.分发饼干：可以感受的出来是贪心题目，先排序，然后尽可能用最小饼干满足剩下的胃口最小的孩子。

## day 18

- 509.斐波那契数：最简单的动态规划题目，没什么好说的。
- 70.爬楼梯：斐波那契数列的一个变体，实际上也是一个非常简单的动态规划题目。
- 746.使用最小花费爬楼梯：稍微描述的有点绕，但是问题不大。
- 62.不同路径：这里只可以向右和向下，就锁定了使用dp的便利性。
- 63.不同路径II：这里加入了障碍之后，初始化的时候要注意，实际计算dp的时候也需要注意。
- 343.整数拆分：这道题目主要是看上去可以递归，但是又不是递归，因此可以考虑使用dp来做。
- 96.不同的二叉搜索树：纯思维题，在纸上面画一画就会了，别的没什么。
- 01bag：0-1背包问题的标准代码，这里应该背过并且理解。然后，应该学习一下如何把一个二维的背包dp数组降维到一维。
- 1049.最后一块石头的重量II：这里用的还是判断是否有任意子集等于目标和的思路，这样是可以的，其实也是可以使用01背包来做的，这样的话就是求sum/2的背包容量下尽可能多的装载石头。

## day 19

- 474.一和零：这道题目是一个多维重量的背包题目，把三维数组的方法以及二维降维数组的方法都写了一遍，感觉还可以。
- complete_bag：单个元素可以使用无限次，这里就不能用一维数组了。
- 518.零钱兑换II：完全背包问题，没什么好说的。
- 322.零钱兑换：这道题也是一个完全背包问题，但是需要加一些条件判断。不过实际上更好的做法就是直接初始化成为max，这个不清楚具体的原理，暂时先不搞。
- 279.完全平方数：这道题可以归结为一个完全背包问题，也可以用一维数组来做，但是原理不太清楚。
- 394.字符串解码：一看到这种括号包裹的，自然而然想到括号匹配，进而想到用栈来解决。
- 338.比特位计数：基本的动态规划题目，这个想了一下就想到了。
- 287.寻找重复数：完全是思维题，记住即可，链表判断环是一样的，但是这里从指针链表变成了数组链表。
- 543.二叉树的直径：后序遍历即可，比较简单的题。
- 3.无重复字符的最长子串：一道很经典的滑动窗口的题目。
- 56.合并区间：其实就是先排序，然后就顺其自然做好了，时间复杂度就是排序的复杂度，这里甚至有点LSM-Tree的意思。
- 53.最大子数组和：这种就是前缀和，其实也是dp的思路。
- 64.最小路径和：这种不需要给出排列的，只能向右或者向左的，就很适合用动态规划来做。

## day 20

- 79.单词搜索：其实这是一个比较简单的深度回溯，但是自己这里用了两层嵌套，实际上只需要一个for循环+dfs即可。
- 32.最长有效括号：这题目是自己想出来的思路，但是不是最优，多遍历了一趟，并且空间也不是最优。
- 72.编辑距离：这道题目还是完全背过吧，不要试图理解了。

